/// <reference types="react" />
import { IPortalCommonProps } from './type';
interface IPortalProps extends IPortalCommonProps {
    id: string;
    modalSet: Set<string>;
    pending: boolean;
    hashId: string;
    initialization: boolean;
    duration: number;
    modalStyle: string;
    spinner?: JSX.Element | boolean;
    spinnerColor: string;
}
declare const Portal: ({ children, id, hashId, modalSet, initialization, pending, isOpen, close, allowClickOutside, duration, modalStyle, spinner, spinnerColor, draggable, }: IPortalProps) => JSX.Element | null;
export default Portal;
