/// <reference types="react" />
import { IAnimation } from './type';
interface IModalProps {
    children: React.ReactNode;
    id: string;
    consumer: React.ReactNode;
    isOpen: boolean;
    setOpen: React.Dispatch<React.SetStateAction<boolean>>;
    allowClickOutside?: boolean;
    duration?: number;
    overlayColor?: string;
    animation?: IAnimation;
    vertical?: number;
    horizontal?: number;
    width?: number;
    height?: number;
    radius?: number;
    backgroundColor?: string;
    asyncOpen?: Function;
    spinner?: JSX.Element | boolean;
    spinnerColor?: string;
    draggable?: boolean;
}
declare const Modal: ({ children, id, consumer, isOpen, setOpen, allowClickOutside, duration, overlayColor, animation, vertical, horizontal, width, height, radius, backgroundColor, asyncOpen, spinner, spinnerColor, draggable, }: IModalProps) => JSX.Element;
export default Modal;
